# Pronouncing

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'pronouncing'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install pronouncing

## Usage
